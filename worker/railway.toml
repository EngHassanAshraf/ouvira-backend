[build]
builder = "nixpacks"
buildCommand = "pip install -r ../requirements.txt"

# cache bust: 5

[deploy]
startCommand = "cd /app/backend && celery -A config worker --loglevel=info --queues=default,fast --concurrency=2"
