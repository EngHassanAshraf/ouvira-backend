[build]
builder = "nixpacks"
buildCommand = "pip install -r ../requirements.txt"

# cache bust: 4

[deploy]
startCommand = "chmod +x ../docker/download_geoip.sh && ../docker/download_geoip.sh /app/backend/geoip && cd /app/backend && celery -A config worker --loglevel=info --queues=default,fast --concurrency=2"
