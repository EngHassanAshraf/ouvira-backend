# This file is intentionally empty.
# All recruitment business logic lives in:
#   application/services/   — write operations (commands)
#   infrastructure/persistence/selectors.py — read operations (queries)
