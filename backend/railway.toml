[build]
builder = "dockerfile"
dockerfilePath = "docker/Dockerfile"
buildArgs = { MAXMIND_LICENSE_KEY = "${{MAXMIND_LICENSE_KEY}}" }

# cache bust: 9

[deploy]
startCommand = "cd /app && python manage.py migrate_schemas --shared && gunicorn config.wsgi:application --config gunicorn.conf.py"
healthcheckPath = "/health/"
