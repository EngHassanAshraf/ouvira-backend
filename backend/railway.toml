[build]
builder = "dockerfile"
dockerfilePath = "docker/Dockerfile"
buildArgs = { MAXMIND_LICENSE_KEY = "${{MAXMIND_LICENSE_KEY}}" }

# cache bust: 11

[deploy]
startCommand = "gunicorn config.wsgi:application --config gunicorn.conf.py"
healthcheckPath = "/health/"
